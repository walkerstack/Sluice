# haiflow server port
PORT=3333

# Prefix for tmux session names (sessions become: <prefix>-<session-name>)
TMUX_PREFIX=claude

# Directory for session state, queues, and responses
# HAIFLOW_DATA_DIR=/tmp/haiflow

# Used by hook scripts to reach the server (defaults to PORT if unset)
# HAIFLOW_PORT=3333

# n8n API key for workflow integration
N8N_API_KEY=
