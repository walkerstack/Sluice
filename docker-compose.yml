services:
  haiflow:
    build: .
    ports:
      - "${PORT:-3333}:${PORT:-3333}"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - PORT=${PORT:-3333}
      - TMUX_PREFIX=${TMUX_PREFIX:-claude}
    tty: true
    restart: unless-stopped
